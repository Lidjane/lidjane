#include <stdio.h>
int main()
{
	int lado, lado2, area;
	printf("informe os dois lados do quadrado\n");
	scanf("%d %d", &lado, &lado2);
    area=lado*lado2;
printf(" o valor da area e: %i", area);	
printf("\n");
	return 0;
}